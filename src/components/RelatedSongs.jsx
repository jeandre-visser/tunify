import SongBar from './SongBar';

const RelatedSongs = ({ isPlaying, data, activeSong, handlePauseClick, handlePlayClick }) => (
  <div className="flex flex-col">
    <h1 className="font-bold text-3xl text-[#FFF]">Related Songs</h1>
  </div>
);

export default RelatedSongs;
